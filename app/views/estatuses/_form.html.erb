<%= form_for(@estatus) do |f| %>
  <% if @estatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estatus.errors.count, "error") %> prohibited this estatus from being saved:</h2>

      <ul>
      <% @estatus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
