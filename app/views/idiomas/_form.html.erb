<%= form_for(@idioma) do |f| %>
  <% if @idioma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idioma.errors.count, "error") %> prohibited this idioma from being saved:</h2>

      <ul>
      <% @idioma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
